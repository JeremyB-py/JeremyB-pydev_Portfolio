{
  "name": "jeremyb-portfolio-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate:pages": "node scripts/generate-project-pages.mjs",
    "shell:css": "node scripts/build-shell-css.mjs",
    "prebuild": "node scripts/build-shell-css.mjs && node scripts/generate-project-pages.mjs",
    "dev": "node scripts/build-shell-css.mjs && node scripts/generate-project-pages.mjs && vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "marked": "^15.0.4",
    "typescript": "~5.6.3",
    "vite": "^6.0.3"
  }
}
