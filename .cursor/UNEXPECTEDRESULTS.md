# Unexpected results log

This file records **surprising**, **confusing**, or **anomalous** outcomes from multi-agent Task work so coordinators and humans can notice patterns over time.

- **Writer:** **`cataloger-agent`** only (see **`@unexpected-results-catalog`**).
- **Inputs:** Typically **`coordinator_alerts[]`** from subagent JSON envelopes (see **`@unexpected-coordinator-alert`**) or explicit coordinator/user requests.

## Incidents

<!-- cataloger-agent appends dated entries below -->
